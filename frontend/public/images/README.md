# 이미지 자산

## 샘플 서류 이미지 (홈 랜딩 "서류 예시" 섹션)

- `sample-certificate.png` — 수료증 예시
- `sample-counseling.png` — 심리상담 의견서 예시
- `sample-pledge.png` — 서약서 예시

> ⚠️ 현재 파일은 **자리표시(placeholder)** 입니다.
> Pillow 로 자동 생성한 KCPEC 로고 + 캡션이 들어간 단순 그림이며,
> 실제 발급 서류와는 다릅니다.
>
> 디자인 확정 후 동일한 파일명으로 교체해 주세요. (frontend 빌드 변경 불필요)
>
> 재생성: `backend/venv/bin/python` 으로 `Pillow` 가 설치된 환경에서
> `frontend/public/images/` 에 PNG 를 만들면 됩니다.
